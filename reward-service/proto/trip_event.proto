syntax = "proto3";

package reward.event;

option go_package = "reward-service/internal/pb/event;eventpb";

// Event เมื่อ trip จบแล้ว
message TripEvent {
  string trip_id = 1; // ใช้เป็น RefID
  string user_id = 2;
  int64  point   = 3;
}
